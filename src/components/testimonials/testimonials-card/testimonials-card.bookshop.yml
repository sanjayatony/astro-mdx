# Metadata about this component, to be used in the CMS
spec:
  structures:
    - content_blocks
  label: Testimonials Cards
  description: Testimonials Cards
  icon: "bolt"
  tags: ["testimonials", "card"]

# Defines the structure of this component, as well as the default values
blueprint:
  component_id:
  heading: "Testimonials"
  content_html: "Explore the whole collection of open-source web components and elements built with the utility classes from Tailwind"
  testimonials:
    - image:
      name:
      position:
      title:
      description:

preview:

# Any extra CloudCannon inputs configuration to apply to the blueprint
_inputs:
  testimonials:
    type: array
